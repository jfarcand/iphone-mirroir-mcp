{
  "name": "mirroir-mcp-website",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node ../scripts/generate-skills-index.mjs || true",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.2.4"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  }
}
